[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
molecule = {extras = ["docker"], version = "*"}

[packages]

[requires]
python_version = "3.8"

[scripts]
test-home-assistant = "bash -c 'cd roles/home-assistant && molecule test --all'"
